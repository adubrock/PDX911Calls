%h1 Listing Calls

<!-- .search -->
<!-- = render 'shared/search' -->

<!-- .map -->
<!-- = render 'shared/map' -->

%table.calls
  %tr
    %th Type
    %th Address

  - @calls.each do |call|
    %tr
      %td= call.call_type
      %td= call.address
